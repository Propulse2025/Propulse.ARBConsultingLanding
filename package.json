{
  "name": "arb-consulting-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "gulp build",
    "deploy": "gulp build && gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "gulp": "^5.0.1"
  }
}
